# 标签管理说明

标签不是在网页上直接改的。这个菜谱网站是静态网站，网页负责浏览和筛选；每道菜的标签要在 Obsidian 源文件里手动增减。

## 去哪里改

打开：

`content/recipes/某道菜.md`

在文件最上面的 `tags:` 下面增删标签，例如：

```yaml
tags:
  - 主食类
  - 面食
  - 快手菜
```

保存后运行 `发布到GitHubPages.command`，公网网站才会更新。

## 首页筛选推荐用这些固定标签

- 主食类
- 减脂餐
- 汤羹
- 快手菜
- 下饭菜
- 高蛋白
- 家常菜

## 怎么加一个新标签

直接在某道菜的 `tags:` 下面加一行：

```yaml
  - 新标签名
```

注意前面要保留两个空格和 `-`。例如给油泼面加“辣味”：

```yaml
tags:
  - 主食类
  - 面食
  - 辣味
  - 快手菜
```

## 怎么删一个标签

删除对应那一行即可。比如不想让某道菜属于“快手菜”，删掉：

```yaml
  - 快手菜
```

## 标签和分类的区别

`category:` 是这道菜的主分类，只写一个。

`tags:` 是筛选标签，可以写多个。

建议：

- `category` 负责大类，例如 `汤羹`、`家常快手`、`主食类`。
- `tags` 负责筛选，例如 `高蛋白`、`下饭菜`、`快手菜`、`面食`。
