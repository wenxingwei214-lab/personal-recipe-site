# 打开这里：如何更新菜谱

## 最简单方法：只用一个入口

如果你在 Obsidian 里，请先打开这个笔记：

```text
菜谱工作台
```

它会出现在左侧 `5-个人网站 / 个人菜谱` 下面。

说明：Obsidian 左侧通常不会显示 `.command` 文件，所以你刚才看不到不是你没找到。

如果你在 Finder 里，以后可以双击这个文件：

```text
菜谱工作台.command
```

然后按菜单选：

```text
1. 新建一道菜谱草稿
2. 发布一篇草稿并更新网页
3. 重新生成并打开预览
4. 打开菜谱文件夹
5. 给菜谱配图/换图
```

日常不用记命令，也不用找 `dist` 在哪里。

---

## 更简单：用 AI 菜谱收集箱

如果你不想填字段，就打开：

```text
AI菜谱收集箱
```

把一段话、链接、截图文字、聊天记录粘进去，然后对 Claudian/Codex 说：

```text
处理菜谱收集箱
```

我会先补全成候选菜谱，列出不确定问题和图片方案。你确认后，我再入库。

---

你日常只需要关心 3 个地方。

## 1. 写菜谱

正式菜谱都在：

```text
content/recipes/
```

每一道菜就是一个 Markdown 文件。比如：

```text
content/recipes/tomato-eggs.md
```

## 2. 新增菜谱，最简单的方法

复制这个模板：

```text
content/_templates/新菜谱模板.md
```

粘贴到：

```text
content/recipes/
```

然后改文件名、菜名、食材、步骤。

刚开始不想发布，就保留：

```yaml
draft: true
```

写完要发布时改成：

```yaml
draft: false
```

再运行：

```bash
npm run build
```

然后打开：

```text
dist/index.html
```

## 3. 更快的方法：命令新建草稿

在这个目录下运行：

```bash
npm run new
```

按提示输入菜名、分类、标签、时长，它会自动在 `content/recipes/` 里生成一篇草稿。

## 你最常改的字段

```yaml
title: 菜名
category: 分类
tags: [标签1, 标签2]
cover: /assets/images/图片名.jpg
time: 20
difficulty: 简单
servings: 2
tools:
  - 炒锅
ingredients:
  - name: 鸡蛋
    amount: 3 个
favorite: false
draft: true
```

## 图片怎么换

最省事：打开 `菜谱工作台.command`，选择 `5. 给菜谱配图/换图`。

手动方法：

把图片放进：

```text
assets/images/
```

建议图片名和菜谱文件名保持一致：

```text
content/recipes/tomato-eggs.md
assets/images/tomato-eggs.jpg
```

然后把菜谱里的 `cover` 改成：

```yaml
cover: /assets/images/tomato-eggs.jpg
```

## 常做菜怎么设置

把：

```yaml
favorite: false
```

改成：

```yaml
favorite: true
```

它就会出现在首页「常做菜」。

## 本周菜单

只改这个文件：

```text
content/week-menu.md
```

改完运行：

```bash
npm run build
```

## 不要动的地方

日常不用管这些：

```text
scripts/
src/
dist/
package.json
skills/
```

你只需要写 `content/recipes/` 和放图片到 `assets/images/`。
